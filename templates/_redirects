# Guide pages live at /guide-<slug> (Netlify serves guide-<slug>.html there).
# The old .html addresses redirect permanently to the clean form. Netlify only
# allows a splat at the end of a path, so the build writes one line per guide
# below this header (site/_redirects is generated; edit this template).
/index.html          /                301!
